version: "3.9"

services:
 stealer-app:
 build:
 context: .
 dockerfile: Dockerfile
 container_name: stealer-app
 ports:
 - "8080:80" # safer if port 80 is used on the VPS
 restart: unless-stopped
 environment:
 TZ: "UTC"
